{
  "name": "react-native-airfob-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Airfob mobile access credentials for React Native and Mendix Native — package, example app, and Mendix module",
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "sync-versions": "node scripts/sync-mendix-versions.js",
    "sync-versions:check": "node scripts/sync-mendix-versions.js --check",
    "test": "npm --workspace react-native-airfob run test"
  },
  "engines": {
    "node": ">=18"
  }
}
